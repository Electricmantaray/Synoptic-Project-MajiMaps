<!-- placeholder -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= indexData.title %></title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body class="min-h-screen bg-gradient-to-r from-sky-300 via-blue-500 to-indigo-600 text-white font-sans flex flex-col items-center justify-center px-6 py-10">

  <!-- Header -->
  <h1 class="text-5xl font-extrabold tracking-tight text-yellow-300 drop-shadow-lg mb-6 animate-fade-in">
    <%= indexData.title %>
  </h1>

  <!-- Image -->
  <div class="max-w-md w-full overflow-hidden rounded-3xl shadow-2xl ring-4 ring-white/20 mb-6 bg-white">
    <img src="<%= indexData.imgpath %>" alt="MajiMaps visual" class="object-cover w-full h-64 rounded-t-3xl">
  </div>

  <!-- Description -->
  <div class="max-w-xl text-lg text-center text-white/90 backdrop-blur-md bg-white/10 p-6 rounded-2xl shadow-md">
    <p><%= indexData.description %></p>
  </div>

  <!-- Decorative Element -->
  <div class="absolute bottom-6 text-sm text-white/70 font-light">
    Empowering Makers Valley — One Drop at a Time 💧
  </div>

</body>
</html>
