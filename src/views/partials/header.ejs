<!-- Mobile overlay for navigation -->
<div id="overlay" class="fixed inset-0 bg-black/50 hidden z-40"></div>

<!-- Header -->
<header class="sticky top-0 z-30 w-full flex justify-center px-4 py-2 shadow-md">
  <div class="flex w-full max-w-6xl items-center justify-between rounded-[15px] bg-[#e5e5eb] px-6 py-3 shadow-md">
    <!-- Logo Title -->
    <img
      src="/images/Minimal - MajiMapsIcon.png"
      alt="MajiMaps"
      class="h-[60px] w-auto object-contain"
    />
    <!-- Navigation -->
    <nav class="flex gap-6 text-sm font-medium text-black">
      <a href="#home" class="transition-colors hover:text-blue-600">Home</a>
      <a href="#map" class="transition-colors hover:text-blue-600">Map</a>
      <a href="#report" class="transition-colors hover:text-blue-600">Report</a>
      <a href="#contact" class="transition-colors hover:text-blue-600">Contact</a>
    </nav>
  </div>
</header>